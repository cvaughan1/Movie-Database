# Lab 10

## Movie Database

In this exercise, we will create a list of movies which the user can then query from the command line. We will create a `Movie` class which we can then use in a data structure to hold the movies.
